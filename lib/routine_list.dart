import 'routine_card.dart';

 List<RoutineCard> routineList =[
   const RoutineCard(),
              const RoutineCard(),
              const RoutineCard(),
              const RoutineCard(),
              const RoutineCard(),
              const RoutineCard(),
              const RoutineCard(),
              const RoutineCard(),

];
